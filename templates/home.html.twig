{% extends 'partials/base.html.twig' %}

{% block content %}
  {% set collection = page.collection() %}
  {% if collection.count %}
    <section class="page section-modular">
      <div class="row">
        {% for module in collection %}
          {{ module.content|raw }}
        {% endfor %}
      </div>
    </section>
  {% endif %}
 
  {% include 'partials/button-scroll-menu-kategorie-links-peter-2025.html.twig' %}
   {% include 'partials/was-hat-sich-geaendert-peter-2025.html.twig' %}
{% endblock %}
